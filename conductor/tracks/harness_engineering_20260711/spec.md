# Harness engineering

The approval workflow repository must fail closed when Conductor context,
contract tests, workflow permissions, or publication-boundary controls are
missing. Tenant-dependent deployment remains a separate gated operation.
